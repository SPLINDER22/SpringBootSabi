<!DOCTYPE html>
<html lang="es" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Completar Perfil de Entrenador - SABI</title>
    <!-- Usar los mismos estilos que el registro -->
    <link rel="stylesheet" th:href="@{/css/login.css}">
    <link rel="stylesheet" th:href="@{/css/registro.css}">
    <link rel="stylesheet" th:href="@{/css/entrenador-form.css}">
    <link href="/vendor/fontawesome-free/css/all.min.css" rel="stylesheet" type="text/css">
    <style>
        .form-select, select.form-select {
            border-radius: 0.75rem !important;
            box-shadow: 0 2px 8px rgba(0,0,0,0.07);
            border: 1.5px solid #bfc9d1;
            background-color: #f8f9fa;
            font-size: 1.1rem;
            margin-bottom: 1rem;
            transition: border-color 0.2s, box-shadow 0.2s;
        }
        .form-select:focus {
            border-color: #2563eb;
            box-shadow: 0 0 0 3px rgba(37,99,235,0.12);
            background: #fff;
        }
        .login-form-section {
            background: #fff;
            border-radius: 1.5rem;
            box-shadow: 0 8px 32px rgba(37,99,235,0.08);
            padding: 48px 36px;
        }
        .login-title {
            margin-bottom: 1.5rem;
        }
        .subtitle {
            color: #64748b;
            font-size: 0.95rem;
            margin-bottom: 2rem;
            text-align: center;
        }
        .login-button {
            border-radius: 0.75rem;
            font-size: 1.1rem;
            padding: 0.75rem 0;
            margin-top: 1.2rem;
        }
        .info-box {
            background: #f0f9ff;
            border-left: 4px solid #3b82f6;
            padding: 1rem;
            border-radius: 0.5rem;
            margin-bottom: 1.5rem;
            font-size: 0.9rem;
        }
        .info-box i {
            color: #3b82f6;
            margin-right: 0.5rem;
        }
        .file-upload-wrapper {
            position: relative;
            overflow: hidden;
            display: inline-block;
            width: 100%;
        }
        .file-upload-wrapper input[type=file] {
            position: absolute;
            left: -9999px;
        }
        .file-upload-label {
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1rem;
            background-color: #f8f9fa;
            border: 2px dashed #bfc9d1;
            border-radius: 0.75rem;
            cursor: pointer;
            transition: all 0.3s;
            font-size: 0.95rem;
            color: #64748b;
        }
        .file-upload-label:hover {
            border-color: #2563eb;
            background-color: #f0f9ff;
        }
        .file-upload-label i {
            margin-right: 0.5rem;
            font-size: 1.2rem;
        }
        .file-list {
            margin-top: 0.5rem;
            font-size: 0.85rem;
        }
        .file-item {
            display: flex;
            align-items: center;
            padding: 0.5rem;
            background: #f8f9fa;
            border-radius: 0.5rem;
            margin-bottom: 0.5rem;
        }
        .file-item i {
            color: #ef4444;
            margin-right: 0.5rem;
        }
        .file-item .file-name {
            flex: 1;
            color: #334155;
        }
        .file-item .remove-file {
            color: #ef4444;
            cursor: pointer;
            font-size: 1.2rem;
        }
        .file-item .remove-file:hover {
            color: #dc2626;
        }

        /* Estilos para las especialidades badges */
        .especialidad-badge {
            display: inline-flex;
            align-items: center;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 2rem;
            margin: 0.25rem;
            font-size: 0.9rem;
            font-weight: 500;
            box-shadow: 0 2px 8px rgba(102, 126, 234, 0.3);
            transition: all 0.3s ease;
        }

        .especialidad-badge:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
        }

        .especialidad-badge i {
            transition: all 0.2s;
        }

        .especialidad-badge i:hover {
            transform: scale(1.2);
            color: #fecaca;
        }

        #especialidadesContainer {
            min-height: 50px;
            padding: 1rem;
            background: #f8f9fa;
            border-radius: 0.75rem;
            border: 2px dashed #e2e8f0;
        }

        #especialidadesContainer:has(.especialidad-badge) {
            border-style: solid;
            border-color: #cbd5e1;
            background: #ffffff;
        }

        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
            transition: all 0.3s;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
        }
    </style>
    <link rel="icon" type="image/png" th:href="@{/img/logoF.png}">
</head>
<body class="entrenador-form">
<div class="login-container">
    <div class="login-form-section mx-auto" style="max-width: 600px; width: 100%;">
        <h1 class="login-title">
            <i class="fas fa-user-tie me-2"></i>
            Completar Perfil de Entrenador
        </h1>

        <p class="subtitle">
            Por favor, proporciona la siguiente información para verificar tu perfil como entrenador profesional
        </p>

        <div class="info-box">
            <i class="fas fa-info-circle"></i>
            <strong>¿Por qué necesitamos esta información?</strong><br>
            Estos datos nos ayudan a conectarte con los clientes más adecuados y a garantizar la calidad de nuestros servicios.
        </div>

        <!-- Mensajes de error/éxito -->
        <div th:if="${error}" class="error-message">
            <p style="color: #ef4444; font-size: 0.9rem; margin-bottom: 15px;">
                <i class="fas fa-exclamation-triangle me-2"></i>
                <span th:text="${error}"></span>
            </p>
        </div>

        <form th:action="@{/auth/completar-perfil-entrenador}" method="post" enctype="multipart/form-data" class="login-form">
            <input type="hidden" th:name="${_csrf.parameterName}" th:value="${_csrf.token}" />

            <div class="form-group">
                <label for="especialidades">
                    <i class="fas fa-dumbbell"></i> Especialidades *
                </label>

                <!-- Contenedor de especialidades seleccionadas -->
                <div id="especialidadesContainer" style="margin-bottom: 1rem;">
                    <!-- Aquí se agregarán las especialidades dinámicamente -->
                </div>

                <!-- Select para agregar especialidad -->
                <div class="d-flex gap-2">
                    <select class="form-select" id="especialidadSelect" style="flex: 1;">
                        <option value="">Selecciona una especialidad...</option>
                        <option value="Fuerza y Acondicionamiento">Fuerza y Acondicionamiento</option>
                        <option value="Pérdida de Peso">Pérdida de Peso</option>
                        <option value="Ganancia Muscular">Ganancia Muscular</option>
                        <option value="Yoga">Yoga</option>
                        <option value="Pilates">Pilates</option>
                        <option value="CrossFit">CrossFit</option>
                        <option value="Entrenamiento Funcional">Entrenamiento Funcional</option>
                        <option value="Cardio y Resistencia">Cardio y Resistencia</option>
                        <option value="Deportes Específicos">Deportes Específicos</option>
                        <option value="Rehabilitación">Rehabilitación</option>
                        <option value="Nutrición Deportiva">Nutrición Deportiva</option>
                        <option value="Entrenamiento Personal">Entrenamiento Personal</option>
                        <option value="Powerlifting">Powerlifting</option>
                        <option value="Calistenia">Calistenia</option>
                        <option value="Running">Running</option>
                        <option value="Ciclismo">Ciclismo</option>
                        <option value="Natación">Natación</option>
                        <option value="Boxeo">Boxeo</option>
                        <option value="Artes Marciales">Artes Marciales</option>
                        <option value="Movilidad y Flexibilidad">Movilidad y Flexibilidad</option>
                    </select>
                    <button type="button" class="btn btn-primary" onclick="agregarEspecialidad()" style="white-space: nowrap;">
                        <i class="fas fa-plus"></i> Agregar
                    </button>
                </div>

                <!-- Input hidden que contendrá las especialidades separadas por comas -->
                <input type="hidden" id="especialidades" name="especialidades" required>

                <small style="color: #64748b; font-size: 0.85rem; display: block; margin-top: 0.5rem;">
                    Selecciona tus especialidades una por una. Puedes agregar varias.
                </small>

                <div id="errorEspecialidades" style="color: #ef4444; font-size: 0.85rem; margin-top: 0.5rem; display: none;">
                    Por favor, agrega al menos una especialidad.
                </div>
            </div>

            <div class="form-group">
                <label for="precioMinimo">
                    <i class="fas fa-dollar-sign"></i> Rango de Precio por Sesión *
                </label>
                <div class="row">
                    <div class="col-6">
                        <input type="number"
                               class="form-control"
                               id="precioMinimo"
                               name="precioMinimo"
                               min="0"
                               step="0.01"
                               required
                               placeholder="Precio mínimo">
                        <small style="color: #64748b; font-size: 0.85rem;">Mínimo</small>
                    </div>
                    <div class="col-6">
                        <input type="number"
                               class="form-control"
                               id="precioMaximo"
                               name="precioMaximo"
                               min="0"
                               step="0.01"
                               required
                               placeholder="Precio máximo">
                        <small style="color: #64748b; font-size: 0.85rem;">Máximo</small>
                    </div>
                </div>
                <small style="color: #64748b; font-size: 0.85rem; display: block; margin-top: 0.5rem;">
                    Indica tu rango de precios por sesión/trabajo en COP (pesos colombianos)
                </small>
            </div>

            <div class="form-group">
                <label for="anioInicioExperiencia">
                    <i class="fas fa-calendar-alt"></i> Año de Inicio como Entrenador *
                </label>
                <input type="number"
                       class="form-control"
                       id="anioInicioExperiencia"
                       name="anioInicioExperiencia"
                       min="1950"
                       th:max="${#temporals.year(#temporals.createNow())}"
                       required
                       placeholder="Ej: 2018">
                <small style="color: #64748b; font-size: 0.85rem;">
                    ¿En qué año comenzaste a entrenar profesionalmente? <span id="experienciaCalculada" style="font-weight: 700; color: #667eea;"></span>
                </small>
            </div>

            <div class="form-group">
                <label for="certificaciones">
                    <i class="fas fa-certificate"></i> Certificaciones (Opcional)
                </label>
                <div class="file-upload-wrapper">
                    <input type="file"
                           id="certificaciones"
                           name="certificaciones"
                           accept=".pdf"
                           multiple
                           onchange="handleFileSelect(event)">
                    <label for="certificaciones" class="file-upload-label">
                        <i class="fas fa-cloud-upload-alt"></i>
                        Haz clic para subir archivos PDF de tus certificaciones
                    </label>
                </div>
                <small style="color: #64748b; font-size: 0.85rem; display: block; margin-top: 0.5rem;">
                    Puedes subir uno o varios documentos en formato PDF (máx. 10MB cada uno)
                </small>
                <div id="fileList" class="file-list"></div>
            </div>

            <div class="info-box" style="background: #fef3c7; border-left-color: #f59e0b;">
                <i class="fas fa-shield-alt" style="color: #f59e0b;"></i>
                <small>Toda la información proporcionada será revisada y será visible para los clientes que busquen entrenadores.</small>
            </div>

            <button type="submit" class="btn login-button w-100">
                <i class="fas fa-check-circle me-2"></i>Completar Registro
            </button>
        </form>
    </div>
</div>

<script>
    // Array para almacenar las especialidades seleccionadas
    let especialidadesSeleccionadas = [];

    // Función para agregar especialidad
    function agregarEspecialidad() {
        const select = document.getElementById('especialidadSelect');
        const especialidad = select.value;

        if (!especialidad) {
            alert('Por favor, selecciona una especialidad');
            return;
        }

        // Verificar si ya está agregada
        if (especialidadesSeleccionadas.includes(especialidad)) {
            alert('Esta especialidad ya está agregada');
            return;
        }

        // Agregar al array
        especialidadesSeleccionadas.push(especialidad);

        // Actualizar la vista
        actualizarVistaEspecialidades();

        // Limpiar el select
        select.value = '';

        // Ocultar mensaje de error
        document.getElementById('errorEspecialidades').style.display = 'none';
    }

    // Función para eliminar especialidad
    function eliminarEspecialidad(especialidad) {
        especialidadesSeleccionadas = especialidadesSeleccionadas.filter(e => e !== especialidad);
        actualizarVistaEspecialidades();
    }

    // Función para actualizar la vista de especialidades
    function actualizarVistaEspecialidades() {
        const container = document.getElementById('especialidadesContainer');
        const hiddenInput = document.getElementById('especialidades');

        // Actualizar el input hidden con las especialidades separadas por comas
        hiddenInput.value = especialidadesSeleccionadas.join(', ');

        // Limpiar el contenedor
        container.innerHTML = '';

        // Crear las badges
        if (especialidadesSeleccionadas.length > 0) {
            especialidadesSeleccionadas.forEach(especialidad => {
                const badge = document.createElement('div');
                badge.className = 'especialidad-badge';
                badge.innerHTML = `
                    <span>${especialidad}</span>
                    <i class="fas fa-times" onclick="eliminarEspecialidad('${especialidad}')" style="cursor: pointer; margin-left: 8px;"></i>
                `;
                container.appendChild(badge);
            });
        } else {
            container.innerHTML = '<p style="color: #94a3b8; font-size: 0.9rem; margin: 0;">No has agregado especialidades aún</p>';
        }
    }

    // Validación antes de enviar el formulario
    document.querySelector('form').addEventListener('submit', function(e) {
        if (especialidadesSeleccionadas.length === 0) {
            e.preventDefault();
            document.getElementById('errorEspecialidades').style.display = 'block';
            document.getElementById('especialidadSelect').scrollIntoView({ behavior: 'smooth', block: 'center' });
            return false;
        }
    });

    // Permitir agregar con Enter en el select
    document.getElementById('especialidadSelect').addEventListener('keypress', function(e) {
        if (e.key === 'Enter') {
            e.preventDefault();
            agregarEspecialidad();
        }
    });

    // Inicializar la vista
    actualizarVistaEspecialidades();

    // Calcular años de experiencia automáticamente
    const anioInput = document.getElementById('anioInicioExperiencia');
    const experienciaSpan = document.getElementById('experienciaCalculada');
    
    if (anioInput) {
        anioInput.addEventListener('input', function() {
            const anioInicio = parseInt(this.value);
            const anioActual = new Date().getFullYear();
            
            if (anioInicio && anioInicio >= 1950 && anioInicio <= anioActual) {
                const aniosExp = anioActual - anioInicio;
                experienciaSpan.textContent = `(${aniosExp} ${aniosExp === 1 ? 'año' : 'años'} de experiencia)`;
            } else {
                experienciaSpan.textContent = '';
            }
        });
    }

    function handleFileSelect(event) {
        const fileList = document.getElementById('fileList');
        const files = Array.from(event.target.files);

        fileList.innerHTML = '';

        files.forEach((file, index) => {
            const fileItem = document.createElement('div');
            fileItem.className = 'file-item';
            fileItem.innerHTML = `
                <i class="fas fa-file-pdf"></i>
                <span class="file-name">${file.name} (${(file.size / 1024).toFixed(2)} KB)</span>
                <i class="fas fa-times remove-file" onclick="removeFile(${index})"></i>
            `;
            fileList.appendChild(fileItem);
        });
    }

    function removeFile(index) {
        const input = document.getElementById('certificaciones');
        const dt = new DataTransfer();
        const files = Array.from(input.files);

        files.forEach((file, i) => {
            if (i !== index) {
                dt.items.add(file);
            }
        });

        input.files = dt.files;
        handleFileSelect({ target: input });
    }
</script>
</body>
</html>

